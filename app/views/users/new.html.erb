<% provide(:title, 'Sign up') %>
<h1>ユーザー設定</h1>

<div class="row">
  <div class="span8 offset2">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "登録", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
