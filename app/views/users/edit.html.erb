<% provide(:title, 'Edit user') %>
<h1>プロフィール更新</h1>

<div class="row">
  <div class="span8 offset2">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "更新", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
